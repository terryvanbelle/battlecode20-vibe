package cand39;

import battlecode.common.*;

/** Refinery / vaporator: nothing to decide. Kept as a class so the loop and monitor still run. */
public strictfp class Building extends Robot {
    Building(RobotController rc) { super(rc); }
    @Override protected void turn() throws GameActionException {}
}
